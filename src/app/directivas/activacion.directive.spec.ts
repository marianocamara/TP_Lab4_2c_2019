import { ActivacionDirective } from './activacion.directive';

describe('ActivacionDirective', () => {
  it('should create an instance', () => {
    const directive = new ActivacionDirective();
    expect(directive).toBeTruthy();
  });
});
